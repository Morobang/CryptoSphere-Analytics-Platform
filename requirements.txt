# Start minimal — install more only when you need it

# API calls and environment variables
requests>=2.31.0
python-dotenv>=1.0.0

# Data manipulation
pandas>=2.0.0
numpy>=1.24.0

# Database (SQL Server)
pyodbc>=5.0.0

# Visualization (for notebooks)
matplotlib>=3.7.0
plotly>=5.15.0

# Jupyter
jupyter>=1.0.0
ipykernel>=6.25.0
